services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: equitr
      POSTGRES_PASSWORD: equitr
      POSTGRES_DB: equitr
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U equitr -d equitr"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: "postgresql://equitr:equitr@postgres:5432/equitr"
      AUTH_SECRET: "${AUTH_SECRET:-change-this-to-a-random-32-char-string-in-production}"
      APP_URL: "${APP_URL:-http://localhost:3000}"
      EMAIL_PROVIDER: "${EMAIL_PROVIDER:-console}"
      RESEND_API_KEY: "${RESEND_API_KEY:-}"
      EMAIL_FROM: "${EMAIL_FROM:-}"
    restart: unless-stopped

volumes:
  postgres-data:
